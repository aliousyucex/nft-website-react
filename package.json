{
  "name": "ice-water-fire-monorepo",
  "version": "1.0.0",
  "description": "Ice Water Fire - Blockchain Card Game Monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "contract"
  ],
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install && cd ../contract && npm install",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "compile:contracts": "cd contract && npx hardhat compile",
    "test:contracts": "cd contract && npx hardhat test",
    "deploy:testnet": "cd contract && npx hardhat run scripts/deploy.ts --network abstractTestnet",
    "deploy:mainnet": "cd contract && npx hardhat run scripts/deploy.ts --network abstractMainnet",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f"
  },
  "keywords": [
    "blockchain",
    "gamefi",
    "card-game",
    "nft",
    "web3",
    "abstract-chain"
  ],
  "author": "Ice Water Fire Team",
  "license": "MIT",
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  }
}

